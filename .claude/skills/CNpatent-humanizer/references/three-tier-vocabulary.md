# 中文专利 3 级词汇模型

> 本文档定义专利场景下的三级词汇检测系统，借鉴 [avoid-ai-writing](https://github.com/conorbronsdon/avoid-ai-writing)
> 的 Tier 1/2/3 模型并完全中文化、专利化。
>
> **核心思想**：不是所有 AI 痕迹词都应该一刀切。有些词单次出现就是问题
> （Tier 1），有些词只在聚集时是问题（Tier 2），有些常见词只在密度过高时
> 才是问题（Tier 3）。这样可以**大幅减少误报**——单个"基于"不是 AI 痕迹，
> 但全文 50 处"基于"就可能是。

---

## Tier 1：必替词（Always Flagged）

任何一处命中即标记。这些词在专利文本中**几乎不应出现**，必须替换或删除。
约 80 个条目，按子类组织。

### 1.1 推销/夸大类（25 词）

| 词 | 替换 |
|---|---|
| 显著提升 | 提高 |
| 显著提高 | 提高 |
| 显著改善 | 改善 |
| 大幅提升 | 提高 |
| 大幅降低 | 降低 |
| 极大提升 | 提高 |
| 卓越的 | 较高的 |
| 优异的 | 较高的 |
| 出色的 | 较高的 |
| 颠覆性的 | (删除) |
| 革命性的 | (删除) |
| 突破性的 | (删除) |
| 划时代的 | (删除) |
| 开创性的 | (删除) |
| 创新性地 | (删除) |
| 巧妙地 | (删除) |
| 精妙地 | (删除) |
| 完美解决 | 解决 |
| 完美实现 | 实现 |
| 高效地 | (删除) |
| 高效的 | (删除) |
| 精准地 | (删除) |
| 精准的 | (删除) |
| 具有广阔的应用前景 | (删除) |
| 意义深远 | (删除) |

### 1.2 过渡填充词（20 词）

| 词 | 替换 |
|---|---|
| 值得注意的是 | (删除) |
| 需要指出的是 | (删除) |
| 需要强调的是 | (删除) |
| 至关重要 | (删除) |
| 尤为关键 | 关键 |
| 旨在 | 用于 |
| 致力于 | (删除) |
| 得益于 | 基于 |
| 鉴于此 | (删除) |
| 有鉴于此 | (删除) |
| 综上所述 | (删除) |
| 总而言之 | (删除) |
| 总之 | (删除) |
| 毋庸置疑 | (删除) |
| 不言而喻 | (删除) |
| 在此基础上 | (删除) |
| 从本质上讲 | (删除) |
| 从根本上说 | (删除) |
| 充分利用 | 利用 |
| 充分考虑 | 考虑 |

### 1.3 排比/结构连接词（10 词）

任何一处单独出现即标记，因为它们暗示三段式结构：

| 词 | 替换 |
|---|---|
| 首先 | (删除) |
| 其次 | (删除) |
| 然后 | (删除) |
| 最后 | (删除) |
| 其一 | (删除) |
| 其二 | (删除) |
| 其三 | (删除) |
| 一方面 | (删除) |
| 另一方面 | (删除) |
| 既...又... | (拆为两句) |

### 1.4 学术腔（10 词）

| 词 | 替换 |
|---|---|
| 本文 | 本发明 |
| 实验结果表明 | (删除) |
| 研究发现 | (删除) |
| 研究表明 | (删除) |
| 我们提出 | 本发明提供 |
| 我们 | 本发明 |
| 笔者 | (删除) |
| 作者 | (删除) |
| 据报道 | (删除) |
| 如文献所述 | (删除) |

### 1.5 AI 高频结构模式（15 词）

| 词 | 替换 |
|---|---|
| 进一步地 | (删除) |
| 更为重要的是 | (删除) |
| 值得一提的是 | (删除) |
| 具体来说 | (删除) |
| 具体而言 | (删除) |
| 与此同时 | (删除) |
| 由此可以看出 | (删除) |
| 事实上 | (删除) |
| 实际上 | (删除) |
| 换言之 | (删除) |
| 也就是说 | 即 |
| 换句话说 | (删除) |
| 不难发现 | (删除) |
| 由此可见 | (删除) |
| 总的来说 | (删除) |

---

## Tier 2：聚集标记词（Cluster-Flagged）

单次出现**不**标记；同一段落内 ≥ 2 次或 500 字范围内 ≥ 3 次才标记。
约 40 个条目。

### 2.1 中性技术连接词

这些词单独使用是合理的，但密集出现暗示 AI 套话：

| 词 | 聚集判定 |
|---|---|
| 基于 | 段内 ≥3 次 |
| 通过 | 段内 ≥3 次 |
| 利用 | 段内 ≥3 次 |
| 采用 | 段内 ≥3 次 |
| 使用 | 段内 ≥3 次 |
| 实现 | 段内 ≥3 次 |
| 提供 | 段内 ≥3 次 |

### 2.2 模板套路开头

| 词 | 聚集判定 |
|---|---|
| 在...的过程中 | 全文 ≥3 次 |
| 在...的基础上 | 全文 ≥3 次 |
| 随着...的发展 | 全文 ≥2 次 |
| 在...的背景下 | 全文 ≥2 次 |
| 针对...问题 | 全文 ≥4 次 |
| 为了...目的 | 全文 ≥3 次 |
| 为...提供 | 全文 ≥3 次 |
| 为...奠定基础 | 全文 ≥1 次 (即 Tier 1) |

### 2.3 程度副词

| 词 | 聚集判定 |
|---|---|
| 较高的 | 段内 ≥2 次 |
| 较好的 | 段内 ≥2 次 |
| 较强的 | 段内 ≥2 次 |
| 较为 | 段内 ≥2 次 |
| 相对 | 段内 ≥2 次 |

### 2.4 模糊量词

| 词 | 聚集判定 |
|---|---|
| 一定的 | 段内 ≥2 次 |
| 一些 | 段内 ≥2 次 |
| 若干 | 段内 ≥2 次 |
| 多个 | 段内 ≥3 次 |
| 各种 | 段内 ≥2 次 |

### 2.5 并列连接

| 词 | 聚集判定 |
|---|---|
| 同时 | 全文 ≥5 次 |
| 此外 | 全文 ≥4 次 |
| 另外 | 全文 ≥4 次 |
| 而且 | 全文 ≥3 次 |

---

## Tier 3：密度标记词（Density-Flagged）

非常常见的词，不能简单删除。仅在全文密度超过阈值（默认 3%）时才标记。
约 15 个条目。

| 词 | 密度阈值 | 备注 |
|---|---|---|
| 的 | 6% | 中文最常用助词，正常密度 4-5% |
| 是 | 2% | "X 是 Y" 句式过多暗示 AI |
| 在 | 4% | 介词密度过高表示句式单一 |
| 和 | 2% | 并列连词，过多暗示列表过密 |
| 与 | 2% | 同上 |
| 等 | 1% | 列举省略词，过多表示懒惰 |
| 进行 | 1% | 动词名词化的标志 |
| 实现 | 0.8% | 抽象动词，过多暗示空洞 |
| 通过 | 1.5% | 过多暗示因果链显式过密 |
| 利用 | 1% | 过多暗示推销 |
| 系统 | 1% | 过多暗示泛化 |
| 方法 | 1.5% | 过多暗示空洞 |
| 该 | 1% | 过多暗示主语漂移 |
| 所述 | 2.5% (发明内容章节) | 过多即使在发明内容也是问题 |

**特别注意**："所述"在"四、发明内容"中合理使用频率高（2-3%），在
"六、具体实施方式"中应该 < 1%。本表的密度阈值需按章节调整。

---

## 检测代码示例

```python
import re
from collections import Counter

# Tier 1 词表（精简示例）
TIER1_WORDS = {
    '显著提升': '提高',
    '卓越的': '较高的',
    '颠覆性的': '',
    '值得注意的是': '',
    '综上所述': '',
    '首先': '',
    '其次': '',
    '本文': '本发明',
    # ... 完整列表见上
}

# Tier 2 词表
TIER2_WORDS = {
    '基于': {'scope': 'paragraph', 'threshold': 3},
    '通过': {'scope': 'paragraph', 'threshold': 3},
    '在...的过程中': {'scope': 'document', 'threshold': 3},
    '同时': {'scope': 'document', 'threshold': 5},
}

# Tier 3 词表
TIER3_WORDS = {
    '是': 0.02,
    '进行': 0.01,
    '实现': 0.008,
    '所述': 0.025,  # 章节相关
}


def detect_tier1(text):
    """Tier 1 检测：单次出现即标记"""
    hits = []
    for word, replacement in TIER1_WORDS.items():
        for match in re.finditer(re.escape(word), text):
            hits.append({
                'tier': 1,
                'word': word,
                'replacement': replacement,
                'position': match.start(),
            })
    return hits


def detect_tier2(text, paragraphs):
    """Tier 2 检测：聚集才标记"""
    hits = []
    for word, rule in TIER2_WORDS.items():
        if rule['scope'] == 'paragraph':
            for i, para in enumerate(paragraphs):
                count = len(re.findall(re.escape(word), para))
                if count >= rule['threshold']:
                    hits.append({
                        'tier': 2,
                        'word': word,
                        'count': count,
                        'paragraph': i,
                    })
        elif rule['scope'] == 'document':
            count = len(re.findall(re.escape(word), text))
            if count >= rule['threshold']:
                hits.append({
                    'tier': 2,
                    'word': word,
                    'count': count,
                    'scope': 'document',
                })
    return hits


def detect_tier3(text):
    """Tier 3 检测：密度标记"""
    hits = []
    total_chars = len(text)
    if total_chars == 0:
        return hits
    for word, threshold in TIER3_WORDS.items():
        count = len(re.findall(re.escape(word), text))
        density = (count * len(word)) / total_chars
        if density > threshold:
            hits.append({
                'tier': 3,
                'word': word,
                'count': count,
                'density': density,
                'threshold': threshold,
            })
    return hits
```

---

## 与 CNpatent 主词表的关系

CNpatent skill 的 [writing-rules.md](../../CNpatent/references/writing-rules.md)
有更全面的禁用词表（按 6 大类组织）。本文档的 Tier 1 词表是 CNpatent 主词表
的**子集**，但增加了"等级"信息。

集成时，本 skill 的 Tier 1 词表与 CNpatent 主词表合并使用：
- 共享：所有词汇定义
- 本 skill 独有：Tier 2/3 模型、聚集判定、密度阈值

---

## 调优建议

如果在某类专利中误报率高，可以：
1. 把误报词从 Tier 1 降级到 Tier 2/3
2. 调整 Tier 2 的聚集阈值
3. 调整 Tier 3 的密度阈值

调优应该基于真实的专利样本统计，而不是凭直觉。建议 grep 真实代理人撰写
的专利样本，统计每个词的密度，作为基线。
